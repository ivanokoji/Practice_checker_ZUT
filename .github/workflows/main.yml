name: Daily ZUT Practice Checker

on:
  schedule:
    - cron: '0 7 * * *'  # Every day at 7:00 UTC
  workflow_dispatch:

jobs:
  check-practice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - run: python monitor.py
        env:
          TELEGRAM_TOKEN: ${{ secrets.7800506938:AAEm8T5l9Po07tRgfiAWTkmDsVD2WCBwIIE }}
          CHAT_ID: ${{ secrets.656981393 }}
